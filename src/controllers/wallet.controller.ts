import { Request, Response } from "express";

export const walletBalance = async (req: Request & { user?: { userId: number } }, res: Response) => {
    try {

    } catch (error) {

    }
}

export const walletTokens = async (req: Request & { user?: { userId: number } }, res: Response) => {
    try {

    } catch (error) {

    }
}

export const tokenInformation = async (req: Request & { user?: { userId: number } }, res: Response) => {
    try {

    } catch (error) {

    }
}